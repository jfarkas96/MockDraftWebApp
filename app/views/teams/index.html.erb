<h1>Teams</h1>
<table>
    <% @teams.each do |team| %>
        <tr>
            <td><img
                    src=<%= team.picture_link %>
                >
            </td>
            <td><%= link_to team.name, team_path(team) %></td>
        </tr>
    <% end %>
</table>